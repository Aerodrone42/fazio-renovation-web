
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
          
      - name: Install dependencies 📦
        run: npm ci || npm install
        
      - name: Create assets directory 📁
        run: |
          mkdir -p docs/assets
          touch docs/assets/.gitkeep
          echo "Assets directory created before build"
        
      - name: Build 🔧
        run: npm run build
        env:
          NODE_ENV: production
          
      - name: Debug information 🧪
        run: |
          echo "Contents of docs directory:"
          ls -la docs/
          echo ""
          echo "Contents of docs/assets directory:"
          ls -la docs/assets/
          
      - name: Verify build output 🔍
        run: |
          echo "Listing build output files:"
          ls -la docs/
          echo "Listing assets directory:"
          ls -la docs/assets/
          
          # Vérifie si index.js existe
          if [ -f "docs/assets/index.js" ]; then
            echo "✅ index.js existe dans docs/assets/"
          else
            echo "❌ index.js manquant dans docs/assets/"
            # Copie le fichier depuis le dossier dist/assets s'il existe
            if [ -f "dist/assets/index.js" ]; then
              cp dist/assets/index.js docs/assets/
              echo "✅ index.js copié depuis dist/assets/"
            else
              # Créer une redirection d'urgence
              echo "console.log('Erreur: fichier index.js introuvable'); alert('Erreur de chargement du site. Veuillez contacter l'administrateur.');" > docs/assets/index.js
              echo "⚠️ Fichier index.js de secours créé"
            fi
          fi
          
      - name: Ensure directories and files exist 📋
        run: |
          echo "www.sarlfaziolorenzo.fr" > docs/CNAME
          touch docs/.nojekyll
          
          # Vérifier si 404.html existe dans public et le copier si nécessaire
          if [ -f "public/404.html" ]; then
            cp public/404.html docs/404.html
            echo "✅ 404.html copied from public to docs"
          fi
          
          # S'assurer que le script gptengineer est présent dans index.html
          if ! grep -q "gptengineer.js" docs/index.html; then
            echo "⚠️ gptengineer.js script not found in docs/index.html, adding it"
            sed -i 's|<head>|<head>\n    <script src="https://cdn.gpteng.co/gptengineer.js" type="module"></script>|' docs/index.html
          else
            echo "✅ gptengineer.js script found in docs/index.html"
          fi
          
          # Ajouter un script de diagnostic en bas du fichier index.html
          if ! grep -q "Site chargé" docs/index.html; then
            sed -i 's|</body>|<script>console.log("Site chargé: " + new Date().toString());</script></body>|' docs/index.html
            echo "✅ Added diagnostic script to index.html"
          fi
          
      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: docs
          branch: gh-pages
          clean: false  # Pour éviter de supprimer des fichiers importants
